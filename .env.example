# Copy file này thành .env.local và điền IP thực của laptop server.
# .env.local KHÔNG được commit vào git (đã có trong .gitignore).
#
# Các biến này override giá trị mặc định trong src/config.js.
# Sau khi thay đổi, cần restart dev server (npm run dev).

VITE_WS_HOST=localhost
VITE_WS_PORT=8765
